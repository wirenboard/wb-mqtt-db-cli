#compdef wb-mqtt-db-cli

# AUTOMATICALLY GENERATED by https://github.com/tqdm/shtab
# Usage:
# 1) Copy this to a file named _wb-mqtt-db-cli (e.g. ~/.local/share/zsh_completion/_wb-mqtt-db-cli).
# 2) Add the following line to your .zshrc:
#    fpath=(~/.local/share/zsh_completion $fpath)
# See also: https://github.com/zsh-users/zsh-completions/blob/master/zsh-completions-howto.org


_shtab_wb_common_commands() {
  local _commands=(
    
  )
  _describe 'wb-mqtt-db-cli commands' _commands
}

_shtab_wb_common_options=(
  "(- : *)--help[show this help message and exit]"
  {-b,--broker}"[MQTT broker url (default\: unix\:\/\/\/var\/run\/mosquitto\/mosquitto.sock)]:broker_url:"
  "--from[start date (default\: None)]:date_from:"
  "--to[end date (default\: None)]:date_to:"
  "--time-format[strftime-style format string for timestamp formating. Use \"\%s.\%f\" for UNIX timestamp (default\: \%Y-\%m-\%d \%H\:\%M\:\%S.\%f)]:time_format:"
  "--limit[The maximum number of data points to request (default\: 10000)]:limit:"
  "--interval[Min interval between data points (ms) (default\: None)]:min_interval:"
  {-a,--auto-interval}"[Automatically estimate the interval between data points based on \"limit\", \"from\" and \"start\" (default\: False)]"
  "--max-records[Max number of records within interval (default\: None)]:max_records:"
  "--decimal-places[Number of decimal places in value (default\: None)]:decimal_places:"
  {-d,--delimiter}"[CSV field separator (default\: )]:delimiter:"
  {-o,--output-fname}"[Write result to file. \"-\" means stdout (default\: -)]:output_fname:"
  "--timeout[Request timeout (in seconds) (default\: None)]:timeout:"
  "(*):List of channels to request:"
)

# guard to ensure default positional specs are added only once per session
_shtab_wb_common_defaults_added=0


_shtab_wb_common() {
  local context state line curcontext="$curcontext" one_or_more='(*)' remainder='(-)*:' default='*::: :->wb-mqtt-db-cli'

  # Add default positional/remainder specs only if none exist, and only once per session
  if (( ! _shtab_wb_common_defaults_added )); then
    if (( ${_shtab_wb_common_options[(I)${(q)one_or_more}*]} +          ${_shtab_wb_common_options[(I)${(q)remainder}*]} +          ${_shtab_wb_common_options[(I)${(q)default}]} == 0 )); then
      _shtab_wb_common_options+=(': :_shtab_wb_common_commands' '*::: :->wb-mqtt-db-cli')
    fi
    _shtab_wb_common_defaults_added=1
  fi
  _arguments -C -s $_shtab_wb_common_options

  case $state in
    wb-mqtt-db-cli)
      words=($line[1] "${words[@]}")
      (( CURRENT += 1 ))
      curcontext="${curcontext%:*:*}:_shtab_wb_common-$line[1]:"
      case $line[1] in
        
      esac
  esac
}



typeset -A opt_args

if [[ $zsh_eval_context[-1] == eval ]]; then
  # eval/source/. command, register function for later
  compdef _shtab_wb_common -N wb-mqtt-db-cli
else
  # autoload from fpath, call function directly
  _shtab_wb_common "$@"
fi

